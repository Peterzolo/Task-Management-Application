
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{title}}</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      color: #4C4C4C;
    }
    .container {
      width: 500px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      font-size: 28px;
      font-weight: 400;
      margin-top: 30px;
      text-align: center;
    }
    .content {
      font-size: 16px;
      font-weight: 500;
      margin-top: 40px;
      color: #4C4C4C;
    }
    .box {
      width: 496px;
      height: 220px;
      background-color: #F8FAFC;
      text-align: center;
      margin: 20px 0;
      display: table;
    }
    .box img {
      display: inline-block;
      vertical-align: middle;
    }
    .info-section {
      background: #d1d0d3;
      padding: 15px;
      width: 450px;
      border-radius: 10px;
      color: #ffffff;
      font-size: 16px;
    }
    .info-item {
      display: table;
      width: 100%;
      margin: 10px 0;
    }
    .label, .value {
      display: table-cell;
      padding: 5px 0;
    }
    .label {
      text-align: left;
      font-weight: 500;
      color: #ffffff;
    }
    .value {
      text-align: right;
      color: #0bb419;
      font-weight: 600;
    }
  </style>
</head>
<body>
<div class="container">
  <div class="header">{{header}}</div>

  <div class="box">
    <img src="https://res.cloudinary.com/dzj0v4upp/image/upload/v1697798585/signed-contract_2_aa3etx.png" alt="illustration">
  </div>

  <div class="content">{{greeting}}: {{userFullName}}</div>

  <div class="content">
    <p>{{message}}</p>
  </div>

  <div class="info-section">
    <div class="info-item">
      <div class="label">Amount</div>
      <div class="value">{{amount}}</div>
    </div>
    <div class="info-item">
      <div class="label">Booking Dates</div>
      <div class="value">{{bookingDates}}</div>
    </div>
    <div class="info-item">
      <div class="label">Apartment Name</div>
      <div class="value">{{apartmentName}}</div>
    </div>
    <div class="info-item">
      <div class="label">Apartment Type</div>
      <div class="value">{{apartmentType}}</div>
    </div>
  </div>

  {{{footer}}}
</div>
</body>
</html>
